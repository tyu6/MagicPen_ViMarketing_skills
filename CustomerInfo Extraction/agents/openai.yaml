interface:
  display_name: "CustomerInfo Extraction"
  short_description: "Extract customer profile data from dialogue files and export JSON, Excel, and Markdown."
  default_prompt: "Use the OpenClaw skill \"CustomerInfo Extraction\" to extract customer information from a dialogue file and export JSON, Excel, and Markdown for CRM import."
